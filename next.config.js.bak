/** @type {import('next').NextConfig} */
const nextConfig = {
  // 开发环境配置
  experimental: {
    // 启用开发环境代理，绕过跨域限制
    serverActions: {
      allowedOrigins: ['localhost:3001']
    }
  },
  
  // 环境变量
  env: {
    NEXT_PUBLIC_CLOUDBASE_ENV_ID: process.env.NEXT_PUBLIC_CLOUDBASE_ENV_ID,
    NEXT_PUBLIC_CLOUDBASE_REGION: process.env.NEXT_PUBLIC_CLOUDBASE_REGION,
  },
  
  // 图片域名配置
  images: {
    domains: [
      'bluetooth-website-3eyhlocfe7b405-1255523606.tcb.qcloud.la',
      'bluetooth-website-3eyhlocfe7b405-1255523606.tcloudbaseapp.com'
    ]
  }
};

module.exports = nextConfig;
